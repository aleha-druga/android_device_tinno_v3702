allow surfaceflinger pq_service:service_manager find;

allow surfaceflinger debug_prop:property_service set;

# los 14
allow surfaceflinger debugfs:file open;
allow surfaceflinger debugfs:file write;
